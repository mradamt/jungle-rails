<header class="page-header">
  <h1>
    Dashboard
  </h1>
</header>

<div class="panel panel-default items">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Type</th>
        <th>Current number in database</th>
      </tr>
    </thead>
    <tbody>
      <%= render 'item', target: 'products', count: @product_count%>
      <%= render 'item', target: 'categories', count: @category_count%>
      <%= render 'item', target: 'sales', count: @sale_count%>
    </tbody>
  </table>
</div>
